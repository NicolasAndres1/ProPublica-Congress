import './Styles.scss';

const SearchBar = () => {

    return (
        <div className='searchBar'>
            <input 
                placeholder='Search'/>
        </div>
    );
};

export default SearchBar;